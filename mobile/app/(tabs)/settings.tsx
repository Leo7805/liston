import { Text, View } from 'react-native';

export default function SentencesScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-semibold text-slate-900">Settings</Text>
    </View>
  );
}
